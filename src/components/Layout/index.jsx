const Layout = ({ children }) => {
    return (
        <main
      className={"flex min-h-screen flex-col items-center  p-24"}
    >
        {children}
    </main>


    )}

    export default Layout;
     